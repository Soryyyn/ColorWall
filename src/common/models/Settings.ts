/**
 * used as settings template & type
 *
 * @interface Settings
 */
export interface Settings {
  autoLaunch: boolean,
  ditherEnabled: boolean,
  fontEnabled: boolean,
  wallpaperFontSize: number,
  chooseFromFavorites: boolean
}